spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: ustudy.cbjawoubagkb.ap-northeast-2.rds.amazonaws.com
    username: jeongwoon
    password: Wjddns8509!

  jpa:
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    hibernate:
      ddl-auto: none
      format_sql: true
    properties:
      hibernate:
        show_sql: true
        format_sql: true

logging:
  level:
    org:
      hibernate:
        SQL: DEBUG
        type:
          descriptor:
            sql:
              BasicBinder: TRACE

